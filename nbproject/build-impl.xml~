<?xml version="1.0" encoding="UTF-8"?>
<!--
*** PLEASE DO NOT EDIT THIS FILE. This file is auto-generated and managed by NetBeans IDE. ***
***          Any modifications you make here will be overwritten.                           ***
-->
<project xmlns:j2seproject1="http://www.netbeans.org/ns/j2se-project/1"
         xmlns:j2seproject3="http://www.netbeans.org/ns/j2se-project/3"
         xmlns:jaxrpc="http://www.netbeans.org/ns/j2me-jwsdp/1"
         basedir=".."
         default="default"
         name="CinemaMS-impl">

    <property file="nbproject/private/private.properties"/>
    <property file="nbproject/project.properties"/>

    <target name="-pre-init"/>
    <target name="-init-private"/>
    <target name="-init-user"/>
    <target name="-init-project"/>
    <target name="-do-init"/>
    <target name="-post-init"/>
    <target name="-init-check"/>
    <target name="-init-macrodef-property"/>
    <target name="-init-macrodef-javac-with-processors"/>
    <target name="-init-macrodef-javac-without-processors"/>
    <target name="-init-macrodef-javac"/>

    <!-- Clean -->
    <target name="clean" description="Clean build products.">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <!-- Compile -->
    <target name="compile" depends="clean" description="Compile source files.">
        <mkdir dir="${build.classes.dir}"/>
        <javac srcdir="${src.dir}"
               destdir="${build.classes.dir}"
               classpath="${javac.classpath}"
               source="${javac.source}"
               target="${javac.target}"
               encoding="${source.encoding}"
               includeantruntime="false"
               debug="true"/>
    </target>

    <!-- JAR -->
    <target name="jar" depends="compile" description="Build JAR.">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.jar}" manifest="${manifest.file}">
            <fileset dir="${build.classes.dir}"/>
            <zipfileset src="${file.reference.mysql-connector-j.jar}"/>
        </jar>
        <echo message="Created JAR: ${dist.jar}"/>
    </target>

    <!-- Run -->
    <target name="run" depends="compile" description="Run project.">
        <java classname="${main.class}" fork="true">
            <classpath>
                <pathelement path="${run.classpath}"/>
            </classpath>
        </java>
    </target>

    <target name="default" depends="jar"/>

</project>
